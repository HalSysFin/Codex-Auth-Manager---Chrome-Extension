# Configuration

The extension stores:

- manager base URL
- optional bearer token

Set these from the popup or options page.

## Base URL examples

- `http://localhost:8080`
- `https://auth.example.com`
